#include <iostream>

using namespace std;

int main()
{

   int a ;
    float b;

    cout << "By using comparison operators." << endl;
    cout <<"Enter an Integer = "<<endl;
    cin>>a;
    cout << "Enter another Float = "<<endl;
    cin>>b;

    cout << "By using comparison operators." << endl;

    cout<<"(a==b) = " << (a==b)<<endl;
    cout<<"(a!=b) = " << (a!=b)<<endl;
    cout<<"(a>b) = " << (a>b)<<endl;
    cout<<"(a<b) = " << (a<b)<<endl;
    cout<<"(a>=b) = " << (a>=b)<<endl;
    cout<<"(a<=b) = " << (a<=b)<<endl;

    return 0;

}

